package protocol

// Request represents a Gofka network request.
type Request struct {
	// TODO: Define common request fields (e.g., API key, API version, correlation ID)
}

// Response represents a Gofka network response.
type Response struct {
	// TODO: Define common response fields (e.g., correlation ID)
}
